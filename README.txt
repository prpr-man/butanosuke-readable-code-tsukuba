開発言語:C++

spec1の実行方法
1. spec.cppをコンパイルしてください。
  例)c++ spec1.cpp

2. 実行ファイルが生成されると思うので、そちらを実行してください。
  例)./a.out

3. オムライスと表示されます。


spec3の実行方法
1. spec.cppをコンパイルしてください。
  例)c++ spec1.cpp

2. 実行ファイルが生成されると思うので、読み込みたいファイルを引数として実行してください。
  例)./a.out recipe_data.txt

3. txtデータが表示されます。

spec4の実行方法
1. spec.cppをコンパイルしてください。
  例)c++ spec1.cpp

2. 実行ファイルが生成されると思うので、読み込みたいファイルを引数として実行してください。
  例)./a.out recipe_data.txt

3. txtデータが表示されます。


spec5の実行方法
1. spec.cppをコンパイルしてください。
  例)c++ spec1.cpp

2. 実行ファイルが生成されると思うので、読み込みたいファイルを引数として実行してください。
  例)./a.out recipe_data.txt

3. IDとtxtデータが表示されます。


spec6の実行方法
1. spec.cppをコンパイルしてください。
  例)c++ spec1.cpp

2. 実行ファイルが生成されると思うので、読み込みたいファイルを引数として実行してください。
  例)./a.out recipe_data.txt

  その際、レシピIDを指定することができます。
  例)./a.out recipe_data.txt 2

3. レシピIDを指定しなければ全てのIDとtxtデータが、レシピIDを指定していればその
IDをもつtxtデータのみが表示されます。
